<%= form_for(@lib) do |f| %>
  <% if @lib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lib.errors.count, "error") %> prohibited this lib from being saved:</h2>

      <ul>
      <% @lib.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :frame_text %><br />
    <%= f.text_field :frame_text %>
  </div>
  <div class="field">
    <%= f.label :keyword_text %><br />
    <%= f.text_field :keyword_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
