%p#notice= notice

- @keyword_text = @lib.keyword_text.split(',')
- @frame_text = @lib.frame_text.split(',')

- count = 0
- @frame_text.each do |frame_bit|
  = "#{frame_bit} #{@keyword_text[count]}"
  - count = count + 1



= link_to 'Edit', edit_lib_path(@lib)
\|
= link_to 'Back', libs_path
